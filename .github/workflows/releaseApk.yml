name: Release APK

on:
  push:
    branches:
    - main
    - dev/CreateRelease
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: get key
      run: echo "${{secrets.KEY}}" > Key.jks
    - name: get BuildProperties
      run: echo '${{secrets.BUILD_PROPERTIES_KT}}' > ${{ github.workspace }}/buildSrc/src/main/kotlin/BuildProperties.kt
    - name: Make gradlew executable
      run: chmod +x ./gradlew
    - name: Build with Gradle
      run: ./gradlew build
    - name: Build Debug APK
      run: ./gradlew assembleDebug
    - name: Releasing using Hub
      uses: actions/upload-artifact@master
      with:
        release_name: Release ${{github.ref}}
        name: release.apk
        path: '${{ github.workspace }}/app/build/outputs/apk/release/app-release.apk'
